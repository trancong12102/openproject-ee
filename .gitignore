# Local environment — holds SECRET_KEY_BASE and DB password. NEVER commit.
.env
.env.local
.env.*.local

# OS cruft
.DS_Store

# Dev-only compose overlay (bind-mounts plugin sources).
docker-compose.override.yml
